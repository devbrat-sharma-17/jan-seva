# The mode `npm run selftest:prod` builds against. Committed, secret-free.
#
# Deliberately separate from `.env.production`: the deployed build runs in
# demo mode until the backend exists, and the production lockout still has
# to be tested every run rather than only after the go-live flip.
VITE_APP_MODE=production
